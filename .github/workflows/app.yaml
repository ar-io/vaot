name: 🌐 $ARIO Airdrop App

on:
  pull_request:
    paths:
      - 'app/**'
  push:
    branches:
      - develop
      - main
    paths:
      - 'app/**'

defaults:
  run:
    working-directory: app

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello World"
